@model MediaWeb.Models.Media.Serie.SerieIndexViewModel

<div>
    <h1>
        Hier een overzicht van al je series
    </h1>
</div>

<div>
    <span>
        Bekijk je collectie!
    </span>
    @if (Model.Series.Count() == 0)
    {
        <span>
            Het ziet er naar uit dat je nog geen series hebt in je collectie!
            Maak er snel eentje aan!
        </span>
    }
    else
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var media in Model.Series)
                {
                    <tr>
                        <td>@media.Title</td>
                        <td>
                            <a asp-controller="Serie" asp-action="Detail" asp-route-id="@media.Id">Detail</a>
                        </td>
                        <td>
                            <a asp-controller="Serie" asp-action="Edit" asp-route-id="@media.Id">Edit</a>
                        </td>
                        <td>
                            <a asp-controller="Serie" asp-action="Delete" asp-route-id="@media.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>

<div>
    <a asp-controller="Serie" asp-action="Create">Maak een nieuwe serie aan</a>
</div>

<div>
    <a asp-controller="Serie" asp-action="Search">Zoek naar een al bestaande series op onze website</a>
</div>


<div class="align-bottom">
    <a asp-controller="Media" asp-action="Index">Terug naar algemeen overzicht</a>
</div>